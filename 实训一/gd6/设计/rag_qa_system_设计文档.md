# gd6 rag_qa_system 设计文档

## 设计主题
gd6 的设计目标是把“会话连续性”和“召回稳定性”同时往前推。

## 关键变化
- `RetrievalService` 接收 `rrf_k`
- 保留 gd5 的会话记忆能力
- 保留 gd3 以来的规则型 PDF 清洗能力

## 设计思路
先尽量用更稳的召回把正确文本块找出来，再让会话记忆补足省略信息，最后交给 LLM 生成答案。

## 设计价值
- 对同义表述更友好
- 对多轮追问更自然
- 适合作为中期增强版 RAG 方案

## 设计成本
- 参数更多
- 回归测试维度明显增多
